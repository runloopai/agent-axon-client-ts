# Runloop API key for devbox provisioning
RUNLOOP_API_KEY=your_api_key_here

# Anthropic API key - securely proxied via Agent Gateway (required for Claude mode)
# Your key is stored as a temporary Runloop secret and never exposed to the devbox
ANTHROPIC_API_KEY=your_anthropic_api_key_here
